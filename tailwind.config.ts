// Tailwind v4 no usa este archivo — la configuración está en src/app/globals.css con @theme {}
// Este archivo se mantiene vacío para compatibilidad con herramientas que lo detectan.
import type { Config } from 'tailwindcss'
const config: Config = { content: [] }
export default config
